// DOM

// Use querySelector and querySelectorAll to select elements from an HTML structure of your choice. Log the selected elements to the console to verify the selections.

const header = document.querySelector('#head');
console.log(header);

const paragraph = document.querySelectorAll('.paragraph');
console.log(paragraph);

const list = document.querySelectorAll('.list');
console.log(list);

const button = document.querySelector('button');
console.log(button);